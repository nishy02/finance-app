{
  "name": "finance-app",
  "version": "1.0.0",
  "scripts": {
    "install": "cd finance-backend && npm install",
    "start": "cd finance-backend && npm run start:prod"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
