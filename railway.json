{
  "$schema": "https://railway.app/railway-schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd finance-backend && npm install"
  },
  "deploy": {
    "startCommand": "cd finance-backend && npm run start:prod",
    "restartPolicyType": "ON_FAILURE"
  }
}
